# HTCondor pool signing key (.pool-password) — TEMPLATE / guidance only.
#
# The REAL deploy/pegasus-htcondor/.pool-password is a BARE SECRET: a single
# line containing your HTCondor pool signing key — a shared secret used to
# authenticate daemons across the pool. It has NO comments, NO key=value, just
# the secret itself. (These comment lines are guidance and must NOT appear in
# the real file.)
#
# Option A — provide your own:
#     cp deploy/pegasus-htcondor/.pool-password.template \
#        deploy/pegasus-htcondor/.pool-password
#     # then overwrite it with a strong single-line secret, e.g.:
#     openssl rand -base64 24 > deploy/pegasus-htcondor/.pool-password
#     chmod 600 deploy/pegasus-htcondor/.pool-password
#
# Option B — omit the file entirely and let provisioning auto-generate one.
#
# The real .pool-password is gitignored. Never commit it.
<replace-this-entire-file-with-a-single-line-pool-signing-key>
