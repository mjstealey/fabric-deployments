# pegasus-htcondor pool — submit/central-manager node.
# Runs collector + negotiator (CentralManager) AND the schedd (Submit) so the
# same node both manages the pool and is the Pegasus/HTCondor access point.
# This node runs no startd — compute happens on the execute nodes.

use ROLE : CentralManager
use ROLE : Submit
